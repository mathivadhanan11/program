#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int num;
    char ch;
    scanf("%d", &num);
    getchar();
    scanf("%c", &ch);
    printf("Integer of %d is %c\n", num, (char)num);
    printf("Character of %c is %d\n", ch, (int)ch);
    return 0;
}
